{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and test",
            "type": "shell",
            "command": "cmake -B build -S . && cmake --build build && cd build && ctest",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}